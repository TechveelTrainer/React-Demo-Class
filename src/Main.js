import Counter from "./Counter"

const Main = () => {
    return (
    <main>

        <Counter value={0} />
        <Counter value={10}  />
        <Counter  value={20} />
        <Counter value={100}  />
        <Counter value={30}  />
    </main>
    )
}

export default Main